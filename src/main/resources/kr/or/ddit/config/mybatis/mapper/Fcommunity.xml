<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="community">

	<select id="selectAllQna" resultType="fqnaVo">
		SELECT * FROM qna
	</select>

	<!-- 20210309 SHS - Fcommunity 글 리스트 조회 -->
	<select id="selectonemarket" resultType="FcommunityVo"
		parameterType="int">
		SELECT * FROM market WHERE market_no = #{market_no}
	</select>

	<!-- 20210309 SHS - Fcommunity 글 등록 -->
	<insert id="registermarket" parameterType="FcommunityVo">
		INSERT INTO market
		(market_no,writer,head_code,title,item_code,content,price,reg_dt,thumbnail,mobile,hit,use_yn)
		VALUES (
		(SELECT NVL(MAX(market_no),0)+1 from market),
		#{writer},
		#{head_code},
		#{title},
		#{item_code},
		#{content},
		#{price},
		sysdate,
		#{thumbnail},
		#{mobile},
		#{hit},
		#{use_yn}
		);
	</insert>

	<!-- 20210309 모든 공지사항 조회 (경찬) -->
	<select id="selectAllNoticeList" resultType="fnoticeVo">
		select *
		from notice
	</select>

	<!-- 20210312 해당 공지사항 클릭 상세정보 (경찬) -->
	<select id="selectNotice" resultType="fnoticeVo"
		parameterType="Integer">
		SELECT *
		FROM notice
		WHERE notice_no = #{value}
	</select>
	
	<!-- 공지사항 삭제 하는거 03/15 (경찬) -->
	<update id="deleteNotice" parameterType="Integer">
		UPDATE notice set use_yn = 'N'
		WHERE notice_no = #{value}
	</update>
	
	<insert id="insertNotice" parameterType="fnoticeVo">
		INSERT INTO notice
		(notice_no, writer, title,content, reg_dt, file_no, hit, use_yn)
		VALUES (
		(SELECT NVL(MAX(notice_no),0)+1 from notice),
		#{writer},
        #{title},
        #{content},
        sysdate,
        1,
        0,
        'Y'
		)
	</insert>
	
		


</mapper>